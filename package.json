{
  "name": "root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "SNOWPACK_PUBLIC_WSS_PORT=7071 lerna run dev --stream",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write ."
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  },
  "devDependencies": {
    "husky": "^4.3.0",
    "lerna": "^3.22.1",
    "prettier": "^2.1.2",
    "pretty-quick": "^3.1.0"
  },
  "browserslist": "defaults, not IE 11"
}
